import React from 'react';
import ReactDOM from 'react-dom/client';
import PurrintApp from './components/PurrintApp.tsx';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <PurrintApp />
  </React.StrictMode>
);
